# ── Core AI ──────────────────────────────────────────────
anthropic>=0.79.0               # Claude API client (LLM backbone)

# ── Document Parsing ────────────────────────────────────
python-docx>=1.2.0              # .docx style sample reader
PyPDF2>=3.0.0                   # PDF text extraction for style samples

# ── Environment ─────────────────────────────────────────
python-dotenv>=1.0.0            # Load .env for API keys & secrets

# ── HTTP / Search ───────────────────────────────────────
requests>=2.31.0                # HTTP client (Serper, Microsoft Graph)
duckduckgo-search>=8.0.0        # Fallback recruiter search engine

# ── Web UI ──────────────────────────────────────────────
flask>=3.0.0                    # Web framework (HTMX + SSE frontend)

# ── Database ────────────────────────────────────────────
sqlalchemy>=2.0.0               # ORM for SQLite application tracker

# ── Background Jobs ────────────────────────────────────
apscheduler>=3.10.4             # Periodic tasks (email check, decay, analysis)

# ── Email Integration (Gmail) ──────────────────────────
google-auth-oauthlib>=1.2.0     # Gmail OAuth 2.0 flow
google-api-python-client>=2.100.0  # Gmail API (fetch & classify emails)

# ── Email Integration (Outlook — optional) ─────────────
msal>=1.26.0                    # Microsoft identity platform (Azure AD OAuth)

# ── Security ────────────────────────────────────────────
cryptography>=41.0.0            # Fernet symmetric encryption for OAuth tokens
